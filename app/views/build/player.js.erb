var party = document.getElementById('party');
party.innerHTML = "<%= j render partial: 'build/player/player' %>"

var encounter = document.getElementById('encounter');
encounter.innerHTML = "<%= j render partial: 'encounter' %>"