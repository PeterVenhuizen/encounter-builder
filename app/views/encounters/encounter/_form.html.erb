<%= form_with(model: encounter, local: false) do |form| %>
    <%= form.label 'Name of the encounter' %>
    <%= form.text_field :name %>

    <%= form.label 'Description' %>
    <%= form.text_area :description %>

    <%= form.submit 'Create Encounter', class: 'btn easy' %>
<% end %>